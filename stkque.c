#include "monint.h"

/**
 * op_stack -  sets the format of the data to a stack (LIFO).
 * This is the default behavior of the program
 * @stack_t: main function var stack_t
 * idx: line count
 * Return: void
 */
void stack_op(stack_t **stack, unsigned int idx)
{
	    (void)stack;
	    (void)idx;
}
