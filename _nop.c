#include "monint.h"

/**
 * op_nop -  function does nothing
 * @stack: doesnt matter
 * @idx : for nothing
 *
 * Return: void
 */
void op_nop(stack_t **stack, unsigned int idx)
{
	(void) stack;
	(void) idx;
}
